apiVersion: v1
kind: Service
metadata:
  name: billing-db-service
  labels:
    app: billing-db
spec:
  clusterIP: None
  selector:
    app: billing-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
